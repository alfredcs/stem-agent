import { Router } from "express";
import type { IStemAgent } from "@stem-agent/shared";
/**
 * Creates Express router serving the agent card at `/.well-known/agent.json`.
 */
export declare function agentCardRouter(agent: IStemAgent): Router;
//# sourceMappingURL=agent-card-handler.d.ts.map